export const routeTemplate = ({beginDate, endDate, destination}) => {
  return `<div class="trip-info__main">
              <h1 class="trip-info__title">${destination}</h1>

              <p class="trip-info__dates">Mar 18&nbsp;&mdash;&nbsp;21</p>
            </div>`;
};
